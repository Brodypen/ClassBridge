export type Course = {
  courseId: string;
  courseName: string;
  url: string;
};

export type CourseInfo = {
    courseId: string;
    course: {
        url: string;
        name: string;
        intro: string;
};

}